Android release package for the tablet.

Files:
- `adscreen-kiosk-release.apk`

Install on a PC with the tablet connected over USB:

```bash
adb install -r adscreen-kiosk-release.apk
```

If ADB is not in PATH, use the Android SDK platform-tools `adb` binary.